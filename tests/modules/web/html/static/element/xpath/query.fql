LET url = @lab.static.static + '/value.html'
LET page = DOCUMENT(url)

LET element = ELEMENT(page, '.tablesorter')
LET actual = XPATH(element, "//input[contains(@type, 'hidden')]")

T::LEN(actual, 40)

RETURN NONE