LET url = @lab.static.static + "/init-script.html"
LET page = DOCUMENT(url, {
  driver: "cdp",
  initScript: {
    source: "window.__beforeDocument = 'ready'",
    timing: "beforeDocument"
  }
})

RETURN T::EQ(ELEMENT(page, "html").attributes["data-inline-value"], "ready")
