LET url = @lab.static.dynamic + "/#/events"
LET page = DOCUMENT(url, { driver: "cdp" })
LET input = ELEMENT(page, "#press-input")

DISPATCH "press" IN input WITH { key: "Enter" }

WAIT(100)

RETURN T::EQ(INNER_TEXT(page, "#press-content"), "Enter")
