LET doc = DOCUMENT(@lab.static.dynamic + "/#/lists", { driver:"cdp" })

LET child = ELEMENT(doc, ".track-list")
LET parent = child.parentElement

T::NOT::NONE(parent)
T::EQ(parent.attributes.id, "tracks")

RETURN NONE