LET url = @lab.static.dynamic + "/#/pagination"
LET page =  DOCUMENT(url, { driver: "cdp" })

LET items = (
    FOR i IN PAGINATION(page, 'li[class="page-item-next page-item"]')
        RETURN i
)

RETURN T::LEN(items, 5)